{
  "name": "ingredient-check-frontend-logic",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "화면(디자인) 없이 로직만 담은 프론트엔드 레이어. 나중에 UI를 붙일 때 훅(hook)만 가져다 쓰면 됩니다.",
  "peerDependencies": {
    "react": "^18.0.0"
  }
}
